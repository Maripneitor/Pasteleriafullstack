# Server Configuration
PORT=3000
NODE_ENV=development
PUBLIC_APP_URL=http://localhost:5173

# Database Configuration (Docker Service Name: db)
DB_HOST=db
DB_USER=pasteleria_user
DB_PASSWORD=secure_password
DB_NAME=pasteleria_db
DB_PORT=3306
DB_SYNC_MODE=none

# Security
JWT_SECRET=change_this_secret_in_production
BCRYPT_ROUNDS=10
EMAIL_USER=tu-correo@gmail.com
EMAIL_PASS=tu-contraseña-de-aplicacion

# WhatsApp Service (WWEBJS)
W_SESSION_PATH=./.wwebjs_auth

# PDF Generation
# (Optional) Path to specific chrome executable if not using Puppeteer bundled
# CHROME_PATH=/usr/bin/google-chrome

# Dynamic Branding Defaults
DEFAULT_TENANT_NAME=Mi Pastelería
DEFAULT_PRIMARY_COLOR=#ec4899
