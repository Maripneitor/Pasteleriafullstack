<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: sans-serif; margin: 40px; font-size: 10pt; color: #333; }
        h1 { text-align: center; color: #d9534f; border-bottom: 2px solid #f0f0f0; padding-bottom: 10px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 10px; text-align: left; }
        th { background-color: #f2f2f2; font-weight: bold; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        .total-row { font-weight: bold; background-color: #e9ecef; }
        .total-row td { text-align: right; }
    </style>
</head>
<body>
    <h1>Reporte de Comisiones del <%= new Date(date + 'T12:00:00').toLocaleDateString('es-MX', { day: '2-digit', month: 'long', year: 'numeric' }) %></h1>

    <table>
        <thead>
            <tr>
                <th>Número de Folio</th>
                <th>Monto de Comisión (5%)</th>
            </tr>
        </thead>
        <tbody>
            <% commissions.forEach(commission => { %>
                <tr>
                    <td><%= commission.folioNumber %></td>
                    <td>$<%= parseFloat(commission.amount).toFixed(2) %></td>
                </tr>
            <% }); %>

            <% if (commissions.length > 0) { %>
                <tr class="total-row">
                    <td>TOTAL</td>
                    <td>$<%= commissions.reduce((sum, item) => sum + parseFloat(item.amount), 0).toFixed(2) %></td>
                </tr>
            <% } else { %>
                <tr>
                    <td colspan="2" style="text-align: center;">No se generaron comisiones en esta fecha.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>