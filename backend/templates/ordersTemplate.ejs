<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: sans-serif; margin: 20px; font-size: 10.5pt; }
        .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 25px; }
        .card { 
            border: 2px dashed #999; padding: 15px; page-break-inside: avoid; 
            display: flex; flex-direction: column; background: #fff;
        }
        .folio-header { text-align: center; border-bottom: 2px solid #555; padding-bottom: 8px; margin-bottom: 8px; }
        .folio-header h2 { margin: 0; font-size: 1.9em; line-height: 1.1; }
        .folio-header p { margin: 0; font-size: 1em; }
        .card-section { margin-bottom: 8px; }
        .card-section p { margin: 3px 0; }
        .account-details { border-top: 1px solid #ccc; margin-top: auto; padding-top: 8px; }
        .account-details p { display: flex; justify-content: space-between; margin: 3px 0; }
        .account-details p span:last-child { font-weight: bold; }
    </style>
</head>
<body>
    <h1>Comandas del Día: <%= new Date(fecha).toLocaleDateString('es-ES', { 
        weekday: 'long', 
        year: 'numeric', 
        month: 'long', 
        day: 'numeric' 
    }) %></h1>
    
    <div class="grid">
        <% folios.forEach(folio => { %>
        <div class="card">
            <div class="folio-header">
                <h2><%= folio.folio %></h2>
                <p><strong>Entrega:</strong> <%= folio.horaEntrega %></p>
            </div>
            <div class="card-section">
                <p><strong>Cliente:</strong> <%= folio.cliente.nombre %></p>
                <p><strong>Tel:</strong> <%= folio.cliente.telefono %></p>
                <p><strong>Dirección:</strong> <%= folio.direccion %></p>
            </div>
            <div class="account-details">
                <p><span>Costo Pastel:</span> <span>$<%= folio.costo.pastel.toFixed(2) %></span></p>
                <p><span>Costo Envío:</span> <span>$<%= folio.costo.envio.toFixed(2) %></span></p>
                <p><span>Adicionales:</span> <span>$<%= folio.costo.adicionales.toFixed(2) %></span></p>
                <p><span>Total:</span> <span>$<%= folio.costo.total.toFixed(2) %></span></p>
                <p><span>Anticipo:</span> <span>$<%= folio.costo.anticipo.toFixed(2) %></span></p>
                <p style="font-weight: bold; font-size: 1.1em; margin-top: 4px;">
                    <span>RESTA:</span> 
                    <span>$<%=(folio.costo.total - folio.costo.anticipo).toFixed(2)%></span>
                </p>
            </div>
        </div>
        <% }); %>
    </div>
</body>
</html>