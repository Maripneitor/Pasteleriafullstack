<!DOCTYPE html>
<html>

<head>
    <style>
        body {
            font-family: 'Helvetica', sans-serif;
            padding: 40px;
            color: #333;
        }

        .header {
            display: flex;
            justify-content: space-between;
            border-bottom: 3px solid <%=(config && config.primaryColor) || '#ec4899' %>;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .logo h1 {
            color: <%=(config && config.primaryColor) || '#ec4899' %>;
            margin: 0;
            font-size: 28px;
        }

        .info {
            text-align: right;
            font-size: 14px;
            color: #666;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        th,
        td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background: <%=(config && config.primaryColor) ? config.primaryColor + '20': '#fbcfe8' %>;
            /* 20% opacity */
            color: <%=(config && config.primaryColor) || '#831843' %>;
        }

        .section-title {
            font-size: 18px;
            font-weight: bold;
            color: <%=(config && config.primaryColor) || '#8b5cf6' %>;
            margin-top: 20px;
            margin-bottom: 10px;
            border-left: 5px solid <%=(config && config.primaryColor) || '#8b5cf6' %>;
            padding-left: 10px;
        }

        .footer {
            margin-top: 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-top: 2px solid #ddd;
            padding-top: 20px;
        }

        .totals {
            font-size: 16px;
        }

        .total-row {
            font-weight: bold;
            font-size: 20px;
            color: #ec4899;
        }

        .qr-section {
            text-align: center;
        }

        .qr-section img {
            width: 100px;
            height: 100px;
        }

        .watermark {
            position: fixed;
            inset: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 96px;
            font-weight: 900;
            opacity: .10;
            transform: rotate(-25deg);
            pointer-events: none;
            z-index: 0;
            letter-spacing: 4px;
        }

        .page {
            position: relative;
            z-index: 1;
        }
    </style>
</head>

<body>
    <div class="watermark">
        <%= watermark %>
    </div>

    <div class="page">
        <div class="header">
            <div class="logo">
                <% if (config && config.logoUrl) { %>
                    <img src="<%= config.logoUrl %>" style="max-height: 80px; max-width: 250px;" alt="Logo" />
                    <% if (config.businessName) { %>
                        <h2 style="font-size: 16px; margin: 5px 0 0 0; color: <%= config.primaryColor || '#ec4899' %>;">
                            <%= config.businessName %>
                        </h2>
                        <% } %>
                            <% } else { %>
                                <h1>
                                    <%= (config && config.businessName) || 'La Fiesta 🎂' %>
                                </h1>
                                <p>Pastelería Artesanal</p>
                                <% } %>
                                    <% if (config && config.pdfHeaderText) { %>
                                        <p style="font-size: 10px; color: #666; margin-top: 5px;">
                                            <%= config.pdfHeaderText %>
                                        </p>
                                        <% } %>
            </div>
            <div class="info">
                <p><strong>Folio:</strong> #<%= folio.folio_numero || folio.id %>
                </p>
                <p><strong>Fecha Entrega:</strong>
                    <%= folio.fecha_entrega %> - <%= folio.hora_entrega %>
                </p>
                <p><strong>Estado:</strong>
                    <%= folio.estatus_produccion %>
                </p>
            </div>
        </div>

        <!-- Cliente -->
        <div class="section-title">Datos del Cliente</div>
        <table>
            <tr>
                <th>Nombre</th>
                <td>
                    <%= folio.cliente_nombre %>
                </td>
                <th>Teléfono</th>
                <td>
                    <%= folio.cliente_telefono %>
                </td>
            </tr>
        </table>

        <!-- Especificaciones -->
        <div class="section-title">Especificaciones del Pastel</div>
        <table>
            <tr>
                <th>Tipo</th>
                <td>
                    <%= folio.tipo_folio %>
                </td>
                <th>Sabores</th>
                <td>
                    <%= (folio.sabores_pan || []).join(', ') %>
                </td>
            </tr>
            <tr>
                <th>Rellenos</th>
                <td>
                    <%= (folio.rellenos || []).join(' , ') %>
                </td>
                <th>Personas</th>
                <td>
                    <%= folio.numero_personas %>
                </td>
            </tr>
        </table>

        <!-- Diseño -->
        <div class="section-title">Detalles de Diseño</div>
        <div style="background: #f9fafb; padding: 15px; border-radius: 8px; margin-bottom: 20px;">
            <p>
                <%= folio.descripcion_diseno || ' Sin descripción detallada' %>
                        </p>
    </div>

    <!-- Footer -->
    <div class="footer">
        <div class="qr-section">
            <% if (typeof qrCode !=='undefined' && qrCode) { %>
                <img src="<%= qrCode %>" />
                <p style="font-size: 10px;">Escanear para Ver en Web</p>
                <% } %>
                    <% if (config && config.pdfFooterText) { %>
                        <p style="font-size: 9px; color: #888; white-space: pre-wrap;">
                            <%= config.pdfFooterText %>
                        </p>
                        <% } %>
        </div>
        <div class="totals" style="text-align: right;">
            <p>Total: $<%= folio.total %>
            </p>
            <p style="color: green;">Anticipo: -$<%= folio.anticipo %>
            </p>
            <p class="total-row">Resta a Pagar: $<%= (folio.total - folio.anticipo).toFixed(2) %>
            </p>
        </div>
    </div>
    </div>

</body>

</html>